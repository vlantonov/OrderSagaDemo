apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: ordersagademo
spec:
  # Payment is Kafka-only; headless service provides DNS without load-balancing.
  clusterIP: None
  selector:
    app: payment-service
